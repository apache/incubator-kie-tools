
    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;

        $className that = ($className)o;
        $nameTool.resolveEquals($context.currentDataObject)
    }