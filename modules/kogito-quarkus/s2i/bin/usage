#!/usr/bin/env bash
cat <<EOF
This is the quay.io/kiegroup/kogito-quarkus-ubi8 S2I image:
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

s2i build <binary path/URL> quay.io/kiegroup/kogito-quarkus-ubi8 <application image>

Where the binary path/URL is the built artifact that you want to copy to your image.

You can then run the resulting image via:
docker run <application image>
EOF