schema_version: 1
name: org.kie.kogito.image.dependencies
version: "0.12.0-rc1"
description: holds common dependencies across images

execute:
  - script: clean.sh

packages:
  install:
    - file
    - shadow-utils
    - tar
    - gzip

