#!/usr/bin/env bash
cat <<EOF
This is the quay.io/kiegroup/kogito-springboot-ubi8 Runtime image:
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

s2i build <source code path/URL> quay.io/kiegroup/kogito-springboot-ubi8 <application image>

You can then run the resulting image via:
docker run <application image>
EOF
