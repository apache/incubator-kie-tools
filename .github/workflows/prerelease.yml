name: test

on:
  push:
    branches: ["**-prerelease"]
    

jobs:
  runs-on: ubuntu-latest
  prerelease:
    steps:
      - name: Write version to disk
        run: echo $GITHUB_REF_oi > version.txt && TIAGO_VERSION = `cat version.txt`
      - name:
        uses: actions/hello-world-docker-action@master
        with:
          who-to-greet: $TIAGO_VERSION

