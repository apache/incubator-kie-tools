{
  "private": true,
  "name": "@kie-tools-build/build-env",
  "version": "0.0.0",
  "description": "",
  "license": "Apache-2.0",
  "homepage": "https://github.com/kiegroup/kie-tools",
  "repository": {
    "type": "git",
    "url": "https://github.com/kiegroup/kie-tools.git"
  },
  "bugs": {
    "url": "https://github.com/kiegroup/kie-tools/issues"
  },
  "bin": {
    "build-env": "bin.js"
  },
  "main": "dist/index.js",
  "files": [
    "bin.js",
    "dist",
    "src"
  ],
  "scripts": {
    "build-env": "node dist/bin.js",
    "install": "pnpm lint && rimraf dist && tsc -p tsconfig.prod.json && pnpm test && pnpm print",
    "lint": "eslint ./src --ext .ts,.tsx",
    "print": "node dist/bin.js --print-vars && node dist/bin.js --print-env",
    "test": "jest --silent --verbose --passWithNoTests"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^15.0.2",
    "eslint": "^7.26.0",
    "jest": "^26.6.3",
    "rimraf": "^3.0.2",
    "typescript": "^4.4.2"
  }
}