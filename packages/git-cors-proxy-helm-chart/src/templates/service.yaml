apiVersion: v1
kind: Service
metadata:
  name: {{ include "git-cors-proxy-helm-chart.fullname" . }}
  labels:
    {{- include "git-cors-proxy-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if (and (eq .Values.service.type "NodePort")) }}
      nodePort: {{ .Values.service.nodePort }}
      {{- end }}
  selector:
    {{- include "git-cors-proxy-helm-chart.selectorLabels" . | nindent 4 }}
