{
  "name": "@kogito-tooling/notifications",
  "version": "0.9.1",
  "description": "",
  "license": "Apache-2.0",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/kiegroup/kogito-tooling.git"
  },
  "scripts": {
    "lint": "echo 'Linting..'",
    "test": "jest --passWithNoTests",
    "test:clearCache": "jest --clearCache",
    "build:dev": "rm -rf dist && webpack --env dev",
    "build:prod": "yarn run lint && yarn test && rm -rf dist && webpack"
  },
  "dependencies": {
    "@kogito-tooling/i18n": "0.9.1",
    "@kogito-tooling/i18n-common-dictionary": "0.9.1",
    "@kogito-tooling/workspace": "0.9.1"
  },
  "babel": {
    "presets": [
      "@babel/env",
      "@babel/react"
    ]
  },
  "jest-junit": {
    "outputDirectory": "./target"
  }
}
