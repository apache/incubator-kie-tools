{
  "name": "vscode-extension-backend",
  "version": "0.21.0",
  "private": true,
  "displayName": "Backend services for BPMN, DMN and Test Scenario Editors",
  "description": "",
  "license": "Apache-2.0",
  "engines": {
    "vscode": "^1.46.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kiegroup/kie-tools.git"
  },
  "categories": [
    "Other"
  ],
  "publisher": "kie-group",
  "main": "./dist/extension/extension.js",
  "activationEvents": [
    "onLanguage:bpmn",
    "onLanguage:dmn",
    "onLanguage:scesim"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": false
    }
  },
  "contributes": {
    "languages": [
      {
        "id": "bpmn",
        "extensions": [
          ".bpmn",
          ".bpmn2"
        ],
        "aliases": [
          "bpmn",
          "bpmn2",
          "Business Process Model and Notation"
        ]
      },
      {
        "id": "dmn",
        "extensions": [
          ".dmn"
        ],
        "aliases": [
          "dmn",
          "Decision Model and Notation"
        ]
      },
      {
        "id": "scesim",
        "extensions": [
          ".scesim"
        ],
        "aliases": [
          "scesim",
          "Test Scenario"
        ]
      }
    ]
  },
  "scripts": {
    "package:prod": "cross-var \"vsce package  --yarn -o ./dist/vscode-extension-backend_$npm_package_version.vsix\"",
    "compile": "webpack",
    "watch": "webpack",
    "lint": "pnpm run-script-if --bool \"$(build-env global.build.lint)\" --then \"pnpm eslint ./src --ext .ts,.tsx\"",
    "test": "pnpm run-script-if --bool \"$(build-env global.build.test)\" --then \"jest --silent --verbose --passWithNoTests\"",
    "test:it": "echo 'No tests to run.'",
    "build:dev": "rimraf dist && webpack --env dev",
    "build:prod:linux:darwin": "rimraf dist && pnpm lint && webpack && pnpm test && pnpm test:it && pnpm package:prod",
    "build:prod:windows": "rimraf dist && pnpm lint && webpack && pnpm test && pnpm package:prod",
    "build:prod": "run-script-os"
  },
  "dependencies": {
    "@kie-tools-core/backend": "0.21.0",
    "@kie-tools-core/vscode-extension": "0.21.0",
    "@kie-tools/backend-extended-services": "0.21.0"
  },
  "devDependencies": {
    "@kie-tools-core/run-script-if": "0.21.0",
    "@kie-tools-core/webpack-base": "0.21.0",
    "@kie-tools/build-env": "0.21.0"
  }
}
