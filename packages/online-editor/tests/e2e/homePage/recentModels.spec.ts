import { test, expect } from "../__fixtures__/base";

test.describe("Recent models", () => {
  test.skip(true, "https://github.com/kiegroup/kie-issues/issues/544");

  test("should open single file workspace", async () => {});

  test("should open a file from ephemeral workspace", async () => {});

  test("should open a file from git workspace", async () => {});

  test("should be possible to add a file on folder workspace", async () => {});
});
