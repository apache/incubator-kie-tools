{
  "name": "@kie-tools/serverless-workflow-mermaid-editor-assets",
  "version": "0.0.0",
  "description": "",
  "license": "Apache-2.0",
  "keywords": [],
  "homepage": "https://github.com/kiegroup/kie-tools",
  "repository": {
    "type": "git",
    "url": "https://github.com/kiegroup/kie-tools.git"
  },
  "bugs": {
    "url": "https://github.com/kiegroup/kie-tools/issues"
  },
  "types": "index.d.ts",
  "main": "index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build:dev": "pnpm dist",
    "build:prod": "pnpm lint && pnpm dist",
    "dist": "rimraf dist && mkdir dist && pnpm dist:sw-mermaid",
    "dist:sw-mermaid": "mkdir \"dist/sw-mermaid\" && cpr ../serverless-workflow-mermaid-viewer/dist ./dist/sw-mermaid",
    "lint": "echo 'Linting'"
  },
  "devDependencies": {
    "@kie-tools/serverless-workflow-mermaid-viewer": "workspace:*",
    "cpr": "^3.0.1",
    "rimraf": "^3.0.2"
  }
}