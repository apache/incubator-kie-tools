{
  "name": "@kogito-tooling/hub",
  "version": "0.2.10",
  "description": "",
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/kiegroup/kogito-tooling.git"
  },
  "dependencies": {
    "vscode-extension-pack-kogito-kie-editors": "0.2.10",
    "chrome-extension-pack-kogito-kie-editors": "0.2.10",
    "@patternfly/patternfly": "2.40.2",
    "@patternfly/react-core": "3.90.0",
    "@patternfly/react-icons": "3.14.7",
    "@types/react-router": "5.1.1",
    "@types/react-router-dom": "5.1.1",
    "react-router": "5.1.1",
    "react-router-dom": "5.1.1",
    "electron": "7.1.9"
  },
  "scripts": {
    "start": "electron dist",
    "rebuild": "electron-rebuild -f -w sqlite3",
    "lint": "tslint -c ../../tslint.json 'src/**/*.{ts,tsx,js,jsx}'",
    "test": "echo 'No tests to run.'",
    "init": "yarn -f",
    "build": "yarn run lint && yarn test && yarn run build:fast",
    "build:fast": "rm -rf dist && webpack --mode development",
    "build:prod-clean": "rm -rf out/*",
    "build:prod-build": "yarn run build --mode production --devtool none",
    "build:prod-package-linux": "electron-packager . \"Kogito Tooling Hub\" --out=out --platform=linux --arch=x64 --overwrite --icon=./build/icon.icns --ignore=\"^/node_modules|^/out|^/src|^/static|^/LICENSE|^/tsconfig.json|^/webpack.config.js|^/yarn-error.log\" && (cd out && zip -ry \"Kogito Tooling Hub-linux-x64.zip\" \"Kogito Tooling Hub-linux-x64\")",
    "build:prod-package-macos": "electron-packager . \"Kogito Tooling Hub\" --out=out --platform=darwin --arch=x64 --overwrite --icon=./build/icon.icns --ignore=\"^/node_modules|^/out|^/src|^/static|^/LICENSE|^/tsconfig.json|^/webpack.config.js|^/yarn-error.log\" && (cd out && zip -ry \"Kogito Tooling Hub-darwin-x64.zip\" \"Kogito Tooling Hub-darwin-x64\")",
    "build:prod-package-windows": "electron-packager . \"Kogito Tooling Hub\" --out=out --platform=win32 --arch=x64 --overwrite --icon=./build/icon.ico --ignore=\"^/node_modules|^/out|^/src|^/static|^/LICENSE|^/tsconfig.json|^/webpack.config.js|^/yarn-error.log\" && (cd out && zip -ry \"Kogito Tooling Hub-win32-x64.zip\" \"Kogito Tooling Hub-win32-x64\")",
    "build:prod-linux": "yarn run build:prod-clean && yarn run build:prod-build && yarn run build:prod-package-linux",
    "build:prod-macos": "yarn run build:prod-clean && yarn run build:prod-build && yarn run build:prod-package-macos",
    "build:prod-windows": "yarn run build:prod-clean && yarn run build:prod-build && yarn run build:prod-package-windows",
    "build:prod": "yarn run build:prod-clean && yarn run build:prod-build && yarn run build:prod-package-linux && yarn run build:prod-package-macos && yarn run build:prod-package-windows"
  },
  "babel": {
    "presets": [
      "@babel/env",
      "@babel/react"
    ]
  },
  "devDependencies": {
    "electron-packager": "14.2.0",
    "electron-rebuild": "1.10.0"
  },
  "files": [
    "**/*",
    "build/icon.*"
  ]
}
