{
  "name": "@kie-tools/dashbuilder-client",
  "version": "0.20.0",
  "description": "",
  "license": "Apache-2.0",
  "files": [
    "dist/"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/kiegroup/kie-tools.git"
  },
  "dependencies": {
    "@kie-tools/dashbuilder": "0.20.0"
  },
  "scripts": {
    "copy:dashbuilder": "copyfiles -e \"../**/*/WEB-INF/**/*\" -e \"../**/*/ace/**/*\" -u 6 \"../dashbuilder/dashbuilder-runtime-parent/dashbuilder-runtime-client/target/dashbuilder-runtime-client/**/*\" ./dist/",
    "build:dev": "rimraf dist && pnpm copy:dashbuilder",
    "build:prod": "pnpm build:dev"
  },
  "devDependencies": {
    "@kie-tools-core/run-script-if": "0.20.0",
    "@kie-tools/build-env": "0.20.0"
  },
  "babel": {
    "presets": [
      [
        "@babel/env",
        {
          "targets": {
            "node": "current"
          }
        }
      ],
      "@babel/react"
    ]
  }
}
