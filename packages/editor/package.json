{
  "name": "@kogito-tooling/editor",
  "version": "0.9.1",
  "description": "",
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/kiegroup/kogito-tooling.git"
  },
  "dependencies": {
    "@kogito-tooling/backend": "0.9.1",
    "@kogito-tooling/envelope": "0.9.1",
    "@kogito-tooling/envelope-bus": "0.9.1",
    "@kogito-tooling/guided-tour": "0.9.1",
    "@kogito-tooling/i18n": "0.9.1",
    "@kogito-tooling/keyboard-shortcuts": "0.9.1",
    "@kogito-tooling/notifications": "0.9.1",
    "@kogito-tooling/workspace": "0.9.1"
  },
  "scripts": {
    "lint": "echo 'Linting..'",
    "test": "jest --silent --verbose",
    "test:clearCache": "jest --clearCache",
    "build:dev": "rm -rf dist && webpack",
    "build": "yarn run lint && yarn test && yarn run build:dev",
    "build:prod": "yarn run build #mode production"
  },
  "babel": {
    "presets": [
      "@babel/env",
      "@babel/react"
    ]
  }
}
