{
  "name": "@redhat/editor",
  "version": "0.8.1",
  "description": "",
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/kiegroup/kogito-tooling.git"
  },
  "dependencies": {
    "@redhat/backend": "0.8.1",
    "@redhat/envelope": "0.8.1",
    "@redhat/envelope-bus": "0.8.1",
    "@redhat/guided-tour": "0.8.1",
    "@redhat/i18n": "0.8.1",
    "@redhat/keyboard-shortcuts": "0.8.1",
    "@redhat/workspace": "0.8.1"
  },
  "scripts": {
    "lint": "tslint -c ../../tslint.json 'src/**/*.{ts,tsx,js,jsx}'",
    "test": "jest --silent --verbose",
    "test:clearCache": "jest --clearCache",
    "build:fast": "rm -rf dist && webpack",
    "build": "yarn run lint && yarn test && yarn run build:fast",
    "build:prod": "yarn run build --mode production --devtool none",
    "pack:productization": "yarn pack"
  },
  "babel": {
    "presets": [
      "@babel/env",
      "@babel/react"
    ]
  }
}