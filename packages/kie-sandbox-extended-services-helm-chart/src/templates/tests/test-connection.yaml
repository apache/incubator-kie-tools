apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kie-sandbox-extended-services-helm-chart.fullname" . }}-test-connection"
  labels:
    {{- include "kie-sandbox-extended-services-helm-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kie-sandbox-extended-services-helm-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
