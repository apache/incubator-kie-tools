{
  "name": "@kie-tools-core/backend",
  "version": "0.21.0",
  "description": "",
  "license": "Apache-2.0",
  "files": [
    "dist",
    "src"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/kiegroup/kie-tools.git"
  },
  "dependencies": {
    "@kie-tools-core/i18n": "0.21.0",
    "@kie-tools-core/notifications": "0.21.0",
    "@kie-tools-core/workspace": "0.21.0",
    "axios": "^0.27.2",
    "fast-xml-parser": "^3.17.4",
    "portfinder": "^1.0.27"
  },
  "devDependencies": {
    "@kie-tools-core/run-script-if": "0.21.0",
    "@kie-tools/build-env": "0.21.0"
  },
  "scripts": {
    "lint": "pnpm run-script-if --bool \"$(build-env global.build.lint)\" --then \"pnpm eslint ./src --ext .ts,.tsx\"",
    "test": "pnpm run-script-if --bool \"$(build-env global.build.test)\" --then \"jest --silent --verbose --passWithNoTests\"",
    "build:dev": "rimraf dist && tsc -p tsconfig.dev.json",
    "build:prod": "pnpm lint && rimraf dist && tsc -p tsconfig.prod.json && pnpm test"
  },
  "babel": {
    "presets": [
      [
        "@babel/env",
        {
          "targets": {
            "node": "current"
          }
        }
      ],
      "@babel/react"
    ]
  }
}
