{
  "name": "@kie-tools/openjdk11-mvn-image",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/kiegroup/kie-tools.git"
  },
  "scripts": {
    "cleanup": "rimraf dist-dev && mkdir dist-dev",
    "install:mvnw": "cd dist-dev/ && mvn wrapper:wrapper",
    "copy:pom": "cp pom.xml dist-dev/",
    "copy:assets": "pnpm copy:pom",
    "podman:build": "pnpm run-script-if --bool $([ $(command -v podman) ] && echo true || echo false) --then \"podman build --ulimit nofile=5000:5000 $(echo $(build-env openJdk11MvnImage.buildTags) | xargs printf -- \"-t $(build-env openJdk11MvnImageEnv.registry)/$(build-env openJdk11MvnImageEnv.account)/$(build-env openJdk11MvnImageEnv.name):%s\n\" | xargs echo) --build-arg OKD_VERSION=$(build-env openJdk11MvnImage.okdVersion) -f Containerfile\" --else \"echo Podman not found, skipping image build.\"",
    "build:dev": "echo Nothing to do",
    "build:prod:win32": "echo \"Build not supported on Windows\"",
    "build:prod:darwin:linux": "pnpm cleanup && pnpm copy:assets && pnpm install:mvnw && pnpm run-script-if --bool \"$(build-env global.build.docker)\" --then \"pnpm podman:build\"",
    "build:prod": "run-script-os"
  },
  "devDependencies": {
    "@kie-tools/build-env": "workspace:*",
    "@kie-tools-core/run-script-if": "workspace:*",
    "@kie-tools/openjdk11-mvn-image-env": "workspace:*"
  }
}
