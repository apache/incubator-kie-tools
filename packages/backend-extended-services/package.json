{
  "name": "@kogito-tooling/backend-extended-services",
  "version": "0.0.1",
  "description": "",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/kiegroup/kogito-tooling.git"
  },
  "files": [
    "index.js",
    "**/target/*.jar"
  ],
  "dependencies": {
    "@kogito-tooling/build-env": "0.0.1"
  },
  "scripts": {
    "lint": "echo 'Linting'",
    "build": "mvn clean install",
    "build:skipTests": "mvn clean install -DskipTests",
    "build:dev": "yarn build:skipTests",
    "build:prod": "yarn lint && yarn run run-script-if --bool \"$(build-env global.build.test)\" --then \"yarn build\" --else \"yarn build:skipTests\""
  }
}
