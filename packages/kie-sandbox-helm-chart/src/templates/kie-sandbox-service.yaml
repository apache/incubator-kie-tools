apiVersion: v1
kind: Service
metadata:
  name: {{ include "kieSandbox.fullname" . }}
  labels:
    {{- include "kieSandbox.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if (and (eq .Values.service.type "NodePort")) }}
      nodePort: {{ .Values.service.nodePort }}
      {{- end }}
  selector:
    {{- include "kieSandbox.selectorLabels" . | nindent 4 }}
