{{- if .Values.extendedServices.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kie-sandbox-helm-chart.extendedServices.serviceAccountName" . }}
  labels:
    {{- include "kie-sandbox-helm-chart.extendedServices.labels" . | nindent 4 }}
  {{- with .Values.extendedServices.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
