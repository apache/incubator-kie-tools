apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kieSandbox.fullname" . }}-test-connection"
  labels:
    {{- include "kieSandbox.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kieSandbox.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
