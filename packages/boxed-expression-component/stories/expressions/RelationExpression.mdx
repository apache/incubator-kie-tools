import { Meta, Story, Canvas } from "@storybook/blocks";
import * as RelationExpression from "./RelationExpression.stories";

<Meta title="MDX/Relation Expression" of={RelationExpression} />

# Relation Expression

With `MDX` we can include a story for `Checkbox` right in the middle of our
markdown documentation.

<Canvas>
  <Story of={RelationExpression.Base} />
</Canvas>
