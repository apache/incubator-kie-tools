{
  "name": "@kie-tools/kie-editors-standalone",
  "version": "0.0.0",
  "description": "",
  "license": "Apache-2.0",
  "keywords": [],
  "homepage": "https://github.com/kiegroup/kie-tools",
  "repository": {
    "type": "git",
    "url": "https://github.com/kiegroup/kie-tools.git"
  },
  "bugs": {
    "url": "https://github.com/kiegroup/kie-tools/issues"
  },
  "files": [
    "dist/bpmn",
    "dist/common",
    "dist/dmn"
  ],
  "scripts": {
    "build:dev": "rimraf dist && webpack --env dev --config webpack.build-resources.config.js && pnpm build:preprocessor && webpack --env dev --config webpack.package-resources.config.js",
    "build:preprocessor": "node dist/preprocessor/preprocessor.js",
    "build:prod": "pnpm lint && pnpm test && rimraf dist && webpack --config webpack.build-resources.config.js && pnpm build:preprocessor && webpack --config webpack.package-resources.config.js && pnpm test:it",
    "build:productization": "pnpm build:prod",
    "cy:open": "run-script-os",
    "cy:open:darwin:linux": "cypress open --project it-tests --config \"baseUrl=http://localhost:$(build-env standaloneEditors.dev.port)\"",
    "cy:open:win32": "pnpm powershell \"cypress open --project it-tests --config \"baseUrl=http://localhost:$(build-env standaloneEditors.dev.port)\"\"",
    "cy:run": "run-script-os",
    "cy:run:darwin:linux": "cypress run -b chrome --project it-tests --config \"baseUrl=http://localhost:$(build-env standaloneEditors.dev.port)\"",
    "cy:run:win32": "pnpm powershell \"cypress run -b chrome --project it-tests --config \"baseUrl=http://localhost:$(build-env standaloneEditors.dev.port)\"\"",
    "lint": "run-script-if --bool \"$(build-env linters.run)\" --then \"pnpm eslint ./src --ext .ts,.tsx\"",
    "powershell": "@powershell -NoProfile -ExecutionPolicy Unrestricted -Command",
    "postreport": "jrm ./dist-it-tests/junit-transformed.xml \"./dist-it-tests/junit-report*.xml\"",
    "start": "webpack serve --host 0.0.0.0 --config webpack.package-resources.config.js",
    "start:it": "webpack serve --host 0.0.0.0 --config ./it-tests/webpack.config.js",
    "test": "run-script-if --ignore-errors \"$(build-env tests.ignoreFailures)\" --bool \"$(build-env tests.run)\" --then \"jest --silent --verbose --passWithNoTests\"",
    "test:it": "run-script-if --ignore-errors \"$(build-env integrationTests.ignoreFailures)\" --bool \"$(build-env integrationTests.run)\" --then \"pnpm rimraf ./dist-it-tests\" \"pnpm start-server-and-test start:it http-get://0.0.0.0:$(build-env standaloneEditors.dev.port) cy:run\" \"pnpm postreport\""
  },
  "devDependencies": {
    "@kie-tools-core/backend": "workspace:*",
    "@kie-tools-core/editor": "workspace:*",
    "@kie-tools-core/envelope-bus": "workspace:*",
    "@kie-tools-core/guided-tour": "workspace:*",
    "@kie-tools-core/i18n": "workspace:*",
    "@kie-tools-core/notifications": "workspace:*",
    "@kie-tools-core/patternfly-base": "workspace:*",
    "@kie-tools-core/webpack-base": "workspace:*",
    "@kie-tools-core/workspace": "workspace:*",
    "@kie-tools/kie-bc-editors": "workspace:*",
    "@kie-tools/stunner-editors": "workspace:*",
    "@types/minimatch": "^3.0.4",
    "@types/react-router-dom": "^5.1.7",
    "@types/underscore": "^1.11.2",
    "cypress": "^9.5.1",
    "cypress-file-upload": "^5.0.8",
    "cypress-iframe": "^1.0.1",
    "cypress-log-to-output": "^1.1.2",
    "jest-junit": "^14.0.0",
    "junit-report-merger": "^2.2.2",
    "kie-tools-root": "workspace:*",
    "minimatch": "^3.0.4",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^5.2.1",
    "start-server-and-test": "^1.12.1",
    "underscore": "^1.13.1"
  },
  "babel": {
    "presets": [
      [
        "@babel/env",
        {
          "targets": {
            "node": "current"
          }
        }
      ],
      "@babel/react"
    ]
  }
}