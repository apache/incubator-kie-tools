<fieldset<%=props.disabled ? " disabled" : ""%>>
    <legend><%=props.label%></legend>
    <!-- must interate over an array-->
    <button>+</button>
    <div>
        <%=props.children.html%>
    </div>
</fieldset>