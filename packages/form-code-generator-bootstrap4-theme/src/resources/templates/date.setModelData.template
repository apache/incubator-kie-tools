<% if (isListItem) { %>document.getElementById("<%=id%>").value = value ? new Date(value).toISOString().slice(0, 16) : "";<% } else { %>document.getElementById("<%=id%>").value = data?.<%=path%> ? new Date(data?.<%=path%>).toISOString().slice(0, 16) : "";<% } %>