{
  "private": true,
  "name": "@kie-tools/runtime-tools-cypress-ouia",
  "version": "1.0.0",
  "description": "This library provides a set of OUIA cypress commands",
  "license": "Apache-2.0",
  "author": "",
  "homepage": "https://github.com/apache/incubator-kie-tools",
  "repository": {
    "type": "git",
    "url": "https://github.com/apache/incubator-kie-tools.git"
  },
  "bugs": {
    "url": "https://github.com/apache/incubator-kie-tools/issues"
  },
  "types": "index.d.ts",
  "main": "dist/index.js",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build:dev": "rimraf dist && tsc",
    "build:prod": "rimraf dist && pnpm run lint && pnpm run build",
    "lint": "kie-tools--eslint './src/**/*.ts{,x}'"
  },
  "devDependencies": {
    "@kie-tools/eslint": "workspace:*",
    "@kie-tools/tsconfig": "workspace:*",
    "cypress": "^13.5.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.6.2"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "pnpm run format",
      "pnpm run lint"
    ]
  }
}