{
  "name": "@kogito-tooling/guided-tour",
  "version": "0.10.0",
  "description": "Guided Tour component",
  "license": "Apache-2.0",
  "source": "src/index.tsx",
  "scripts": {
    "lint": "echo 'Linting..'",
    "test": "jest --silent --verbose",
    "test:clearCache": "jest --clearCache",
    "build:dev": "rm -rf dist && webpack --env dev",
    "build:prod": "yarn run lint && yarn test && rm -rf dist && webpack",
    "start": "rm -rf dist && webpack --watch"
  },
  "peerDependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "dependencies": {
    "@kogito-tooling/envelope-bus": "0.10.0",
    "@kogito-tooling/i18n": "0.10.0"
  },
  "presets": [
    "@babel/preset-env",
    "@babel/preset-react"
  ],
  "files": [
    "dist"
  ]
}
