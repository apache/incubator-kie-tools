{
  "name": "chrome-extension-pack-kogito-kie-editors",
  "version": "0.9.1",
  "description": "",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/kiegroup/kogito-tooling.git"
  },
  "dependencies": {
    "@kogito-tooling/chrome-extension": "0.9.1",
    "@kogito-tooling/editor": "0.9.1",
    "@kogito-tooling/external-assets-base": "0.9.1",
    "@kogito-tooling/kie-bc-editors": "0.9.1",
    "@kogito-tooling/patternfly-base": "0.9.1"
  },
  "scripts": {
    "lint": "echo 'Linting...'",
    "test": "echo 'No tests to run.'",
    "build:dev": "rm -rf dist && webpack",
    "build": "yarn run lint && yarn test && yarn run build:dev",
    "build:prod": "yarn run build #mode production",
    "serve-envelope": "webpack serve",
    "test:it": "jest --runInBand"
  },
  "babel": {
    "presets": [
      "@babel/env",
      "@babel/react"
    ]
  },
  "jest": {
    "transform": {
      "^.+\\.(ts)$": "ts-jest"
    },
    "testRegex": [
      "it-tests/tests/.*Test.ts"
    ],
    "testTimeout": 100000
  }
}
