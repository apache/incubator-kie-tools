{
  "name": "appformer-js",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "init": "npx lerna bootstrap",
    "build": "npx lerna run build --stream",
    "build:fast": "npx lerna run build:fast --stream",
    "build:prod": "npx lerna exec -- yarn run build --mode production",
    "build:colorful": "npx lerna exec -- yarn run build",
    "start": "docker-compose up --build -d && docker-compose logs -f",
    "stop": "docker-compose down --rmi all",
    "reset": "yarn stop && docker volume rm appformer-js_db-data; yarn start"
  },
  "devDependencies": {
    "lerna": "3.4.0"
  }
}
