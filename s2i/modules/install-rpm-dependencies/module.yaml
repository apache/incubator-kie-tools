schema_version: 1
version: "0.3.0"
name: install-rpm-dependencies

execute:
  - script: clean.sh

# holds common dependencies across images
packages:
  install:
    - shadow-utils
    - tar
    - gzip

