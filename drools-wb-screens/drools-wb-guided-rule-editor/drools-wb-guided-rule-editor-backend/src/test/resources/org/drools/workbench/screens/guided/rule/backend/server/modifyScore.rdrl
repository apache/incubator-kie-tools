package org.drools.workbench.screens.guided.rule.backend.server;

import java.lang.Number;

rule "modifyScore-rule"
	dialect "mvel"
	when
	then
		scoreHolder.addMultiConstraintMatch(kcontext, 123, 456);
end
